// const container = document.querySelector(".colorPicker");
// import colors from "./data/color.json";



// const markup = colors
//   .map((item) => {
  
//     return `<button  type="button" style="background-color:${item.hex}" data-hex="${item.hex}">${item.hex} </button>`;
//   })
//   .join("");
// container.insertAdjacentHTML("beforeend", markup);

// container.addEventListener("click", (event) => {
//   if (event.target.nodeName === "BUTTON") {
    
//     return (document.body.style.backgroundColor = event.target.dataset.hex);
//   }
 
// })